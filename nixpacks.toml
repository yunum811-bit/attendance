[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx vite build"]

[start]
cmd = "npx tsx src/server/index.ts"

[variables]
DATA_DIR = "/data"
UPLOADS_DIR = "/data/uploads"
